const all=LESSONS.flatMap(l=>l.questions.map(q=>({...q,title:l.title})));for(let i=all.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[all[i],all[j]]=[all[j],all[i]]}const qs=all.slice(0,10),box=document.getElementById('quiz');qs.forEach((q,i)=>box.insertAdjacentHTML('beforeend',`<div class="mini-q"><b>${i+1}. ${q.p}</b><input id="a${i}" autocomplete="off"></div>`));const norm=s=>String(s).toLowerCase().replace(/[.!?]/g,"").replace(/\s+/g," ").trim();document.getElementById('grade').onclick=()=>{let n=0,html='';qs.forEach((q,i)=>{const v=document.getElementById('a'+i).value,ok=norm(v)===norm(q.a);if(ok)n++;else html+=`<p class="ng">${i+1}. 正解：${q.a}　${q.e}</p>`});document.getElementById('result').innerHTML=`<h2>${n} / 10問 正解</h2>${n===10?'<p class="ok">全問正解！ 総合確認へ進めます。</p>':html}`};